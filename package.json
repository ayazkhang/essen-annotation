{
  "name": "essen-annotation",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"yarn workspace backend dev\" \"yarn workspace frontend dev\"",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "setup": "yarn install && yarn workspace backend generate:demo && yarn workspace backend prisma:migrate && yarn workspace backend seed",
    "test": "yarn workspace backend test",
    "build": "yarn workspace backend build && yarn workspace frontend build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "yarn@1.22.22"
}
